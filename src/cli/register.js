const config = {
  command: 'register',
  desc: 'Registers an account with Wasabi. (Not working)',

  /**
   * CLI Arguments
   * @param  {Object}  yargs  Yargs instance
   */
  builder: (yargs) => {},

  /**
   * CLI Handler
   * @param  {Object}  argv  CLI Arguments
   */
  handler: async (argv) => {
    // @TODO Registers the things
  }
}

module.exports = config
